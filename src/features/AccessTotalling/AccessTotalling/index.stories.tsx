import { Meta, StoryObj } from '@storybook/react';
import AccessTotalling from 'src/features/AccessTotalling/AccessTotalling';

const meta: Meta<typeof AccessTotalling> = {
  component: AccessTotalling,
};

export default meta;

type Story = StoryObj<typeof AccessTotalling>;

export const Default: Story = {};

import { Meta, StoryObj } from '@storybook/react';
import AccessTotallingDetail from 'src/features/AccessTotalling/AccessTotallingDetail';

const meta: Meta<typeof AccessTotallingDetail> = {
  component: AccessTotallingDetail,
};

export default meta;

type Story = StoryObj<typeof AccessTotallingDetail>;

export const Default: Story = {};

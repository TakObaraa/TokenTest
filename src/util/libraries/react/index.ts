import React from 'react';

export const {
  forwardRef,
  Children,
  Component,
  Fragment,
  StrictMode,
  Suspense,
  createContext,
  createElement,
  createRef,
  isValidElement,
  lazy,
  memo,
  useCallback,
  useContext,
  useDebugValue,
  useEffect,
  useImperativeHandle,
  useLayoutEffect,
  useMemo,
  useReducer,
  useRef,
  useState,
  useId,
} = React;

export default React;

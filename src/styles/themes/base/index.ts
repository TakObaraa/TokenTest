import { borders } from '@styles/themes/base/borders';
import { breakpoints } from '@styles/themes/base/breakpoints';
import { colors } from '@styles/themes/base/colors';
import { radius } from '@styles/themes/base/radius';
import { shadows } from '@styles/themes/base/shadows';
import { sizes } from '@styles/themes/base/sizes';
import { spacings } from '@styles/themes/base/spacings';
import {
  fontSizes,
  fontWeights,
  fonts,
  lineHeights,
} from '@styles/themes/base/typography';
import { zIndex } from '@styles/themes/base/zIndex';

export default {
  borders,
  breakpoints,
  colors,
  radius,
  shadows,
  sizes,
  spacings,
  fonts,
  fontSizes,
  fontWeights,
  lineHeights,
  zIndex,
};
